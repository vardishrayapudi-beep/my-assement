# Explanation for Question 1

This section explains the commands used for environment verification. The `whoami` and `id` commands confirmed the active user and group membership, while `echo "$SHELL"` and `pwd` identified the shell and current working directory. The `ls -1` command showed the files present in the workspace, and `curl` verified that outbound network access was available.
